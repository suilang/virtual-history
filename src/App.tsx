import ReactDOM from 'react-dom';
import { VirtualHistory } from './components/VirtualHistory';
function Root() {
    return <div>124</div>;
}

ReactDOM.render(<Root />, document.getElementById('root'));

export default {};
